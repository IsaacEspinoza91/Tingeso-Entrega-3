spring:
  application:
    name: reportes-service
  cloud:
    config:
      enabled: true
      uri: http://localhost:8081
  datasource:
    url: jdbc:mysql://localhost:3306/reportes_db
    username: root
    password: 1234
  jpa:
    hibernate:
      ddl-auto: update
