spring:
  application:
    name: cliente-desc-frecu-service
  cloud:
    config:
      enabled: true
      uri: http://localhost:8081
  datasource:
    url: jdbc:mysql://localhost:3306/cliente_db
    username: root
    password: 1234
  jpa:
    hibernate:
      ddl-auto: update