server:
  port: 8090
spring:
  cloud:
    config:
      server:
        git:
          default-label: main
          uri: https://github.com/IsaacEspinoza91/Prueba-config
          search-paths: config-data
  application:
    name: config-service

