spring:
  application:
    name: descuento-grupo-service
  cloud:
    config:
      enabled: true
      uri: http://localhost:8081
  datasource:
    url: jdbc:mysql://localhost:3306/descuento_grupo_db
    username: root
    password: 1234
  jpa:
    hibernate:
      ddl-auto: update
